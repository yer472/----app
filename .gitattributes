# 统一换行符：仓库里一律存 LF，避免 Windows 上每次提交都冒出
# "LF will be replaced by CRLF" 的警告，也避免整文件级别的假 diff。
* text=auto eol=lf

# 二进制文件不要做换行符转换
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.webp binary
*.ico binary
