# 学习笔记 APP —— 产品设计文档

| 项目 | 内容 |
| --- | --- |
| 产品名称 | 学习笔记（XXBJ） |
| 文档版本 | v1.2 |
| 创建日期 | 2026-09-19 |
| 文档状态 | **技术方案已全部确定，可进入开发** |
| 目标读者 | 产品需求方（本人）、后续开发协作者 |

### 已确定的关键决策

| # | 决策项 | 结论 | 日期 |
| --- | --- | --- | --- |
| D1 | 技术栈方案 | **方案 A：本地优先的 PWA** | 2026-09-19 |
| D2 | 目标平台 | **Windows 电脑为主**（桌面浏览器 / PWA 独立窗口） | 2026-09-19 |
| D3 | 多端同步 | **不需要** | 2026-09-19 |
| D4 | 思维导图功能 | **暂缓开发**，先完成其他功能（设计中保留，见附录 A） | 2026-09-19 |
| D5 | 笔记正文格式 | **Milkdown —— 存储用 Markdown，编辑用即时渲染** | 2026-09-19 |
| D6 | 标签系统 | 暂不做，先用搜索替代，M5 时再评估 | 2026-09-19 |
| D7 | 备份文件夹形式 | **镜像（普通文件）+ 每日快照 zip（保留 7 天）**；文件夹位置由用户在设置页选，建议指向 OneDrive 同步目录 | 2026-09-19 |
| D8 | 目标浏览器 | **Edge**（Windows 11） | 2026-09-19 |
| D9 | 绘图（机械原理图） | **手写 SVG 画板，零新依赖**。场景 JSON 内嵌在导出 SVG 的 `<metadata>` 里——一个文件既是渲染结果又是源数据，不新建数据表 | 2026-09-20 |
| D10 | 键盘快捷键 | **一份目录（`src/lib/shortcuts/catalog.ts`）同时充当绑定的来源和界面上的说明**——绑定代码只写 id，组合键从目录查，两边不可能对不上。全局层靠 `defaultPrevented` 给编辑器让位、靠浮层计数给对话框让位。完整清单一节放在设置页，`Ctrl+/` 直达 | 2026-09-20 |
| D11 | 机构符号的定义存在哪 | **点符号的定义内联进场景**（`Scene.defs`），两点符号的定义留在代码里。理由是「删掉一个自定义符号，已经画好的图照常打得开」，以及「撤销删符号时定义跟着一起回来」。代价：改一个自定义符号，已画好的图不会跟着变 | 2026-09-20 |

### 开发进度

| 阶段 | 内容 | 状态 |
| --- | --- | --- |
| M0 | 项目骨架：Vite + React + TS + Tailwind、IndexedDB schema、`repository/` 数据访问层、路由与布局 | ✅ 完成 |
| M1 | 科目与章节的增删改查、面包屑导航、级联删除 | ✅ 完成 |
| M2 | 笔记编辑器（Milkdown）、图片粘贴与压缩、自动保存 | ✅ 完成 |
| M3 | 全局搜索、一键导出/导入、自动备份到本地文件夹 | ✅ 完成 |
| M3.5 | PWA：安装为桌面应用、离线可用（Web App Manifest + 手写 Service Worker） | ✅ 完成 |
| M4 | **键盘快捷键**：全局与页面级绑定、设置页里的完整一览（`Ctrl+/` 直达） | ✅ 完成 |
| M4.5 | 深色模式打磨、置顶/最近编辑、性能优化、空状态 | 待开始 |
| M6 | 绘图：自由画板（直线/矩形/椭圆/手绘、网格吸附、撤销重做），画好的图存成 SVG 附件插进正文 | ✅ 阶段 1 完成 |
| M6.5 | 绘图：机构符号库（GB/T 4460 图形特征）、端点吸附、角度约束、机构语义（拖动铰链联动构件）、自定义符号、橡皮 | ✅ 完成 |

> 代码说明见 [README.md](README.md)。开发服务器跑起来后访问 `/dev/db-check` 可一键验证存储层。

#### M2 实施记录

落地时对 §5.4 的方案做了几处细化，记录在此备查：

| 事项 | 决定 | 原因 |
| --- | --- | --- |
| 编辑器 | 用 **Milkdown Crepe**（Milkdown 的完整版）而非核心包 | 底层仍是同一个 ProseMirror，但 Crepe 自带斜杠菜单、块操作手柄、占位提示和图片块，正好是记笔记要用的；用核心包自己拼要写不少代码 |
| 图片存储 | 正文里写 `asset://<附件id>`，渲染时再解析成 blob URL | blob URL 只在当前会话有效，直接存进数据库下次打开就是死链；存附件 id 则永远找得回来 |
| 图片格式 | **输入是 PNG 就输出 PNG，是 JPEG 就输出 JPEG**，不一律转 JPEG | 截图是 PNG，文字边缘用 PNG 存更清晰、体积也常常更小，还能保住透明区域；一律转 JPEG 会让文字截图出现振铃状糊边，这是压缩算法特性，调参解决不了 |
| 自动保存范围 | **标题和正文用同一套防抖机制** | 原本只在失焦时存标题。但"敲完标题直接关标签页"就会丢，改成防抖保存后由统一的落盘逻辑兜住 |
| 包体积 | 笔记页**懒加载** | Milkdown 及其依赖约 1.15MB，但翻科目列表根本用不到编辑器。拆开后首屏从 1.9MB 降到 448KB（M2 时的数字；M3~M4 的功能陆续加回来之后是 522KB，其中 M4 的快捷键层约 8KB） |
| 编辑器主题 | 只引浅色主题，深色靠覆盖 CSS 变量实现 | Crepe 自带的深色主题写死在 `.milkdown` 上，和浅色主题直接引入会互相覆盖；改成 `.dark .note-editor .milkdown` 覆盖变量，权重更高且能跟着应用的深色开关走 |

> ⚠️ **踩过的坑：** 覆盖 Crepe 的 CSS 变量时，选择器**必须**带 `.note-editor` 前缀。Crepe 自己的规则也写在 `.milkdown` 上，权重相同时后加载的赢——而编辑器的样式在懒加载 chunk 里，一定比 `index.css` 后到。少了前缀，覆盖会被静默吃掉（字体覆盖就这么失效过一次，页面不报错、只是悄悄变回默认字体）。

#### M3 实施记录

| 事项 | 决定 | 原因 |
| --- | --- | --- |
| 备份形式 | **镜像 + 每日快照**（决策 D7） | 镜像给的是「当前状态」，误删一次就会被下次备份同步掉；快照给的是「回到过去某一天」。两者互补，缺一个都少一种救命场景 |
| 备份格式 | **一个格式，三处复用**（手动导出 / 每日快照 / 文件夹镜像内容完全一致） | 多套格式就会出现「导出的能恢复、自动备份的不能」这种问题。格式只有一份实现，改动时不会漏掉另一半 |
| 图片存放 | 独立文件 `images/<附件id>.<ext>`，不进 JSON | 塞 base64 会让体积涨 33%，而且用户在文件夹里根本没法直接看这些图 |
| 笔记另存 .md | 同时导出 `笔记/科目/章节/标题.md`，带 front matter | 这是 F5.5「脱离本 App 仍可阅读」的落点。即使这个 App 以后不能用了，笔记还是普通的 Markdown 文件，Obsidian/Typora 直接打开 |
| .md 里的图片 | 改写成相对路径 `../../../images/x.png` | 正文在 3 层目录下、图片在根目录，不算相对路径的话导出的 .md 图片全是死链 |
| 恢复语义 | **覆盖式**，且默认勾选「恢复前先导出当前数据」 | F5.2 要的是「完整恢复」。合并语义会引入 id 冲突、时间戳谁赢等一堆边界问题，对一个单机单用户工具不值得。默认导出那一步是给「选错文件」留的后悔药 |
| 搜索匹配 | 先把正文转成**纯文本**再匹配，不匹配 Markdown 原文 | 直接匹配原文的话，搜 `asset` 会命中每一张图、搜 `##` 会命中每一个标题，结果里全是噪声 |
| 搜索入口 | 侧栏做成「看起来像输入框的按钮」跳到搜索页，而不是真输入框 | 两个输入框同步状态很容易出 bug。做成按钮后状态只有一处（URL 的 `?q=`），点击后自动聚焦，键盘流走 Ctrl+K |
| zip 打包 | 走 fflate 的**流式**接口，不用 `zipSync` | 备份里可能塞几百 MB 图片，同步压完会卡死主线程，且中间数据要在内存里堆成完整一份。流式的能报进度、内存也有界 |
| 镜像目录 | `笔记/` 和 `images/` **只增不删** | 在 App 里删掉一篇笔记，文件夹里的 .md 不清理。这样绝不会误删用户文件；恢复以 data.json 为准，残留文件不影响结果，只是占点空间。已写进 `备份说明.txt` |
| 快照轮转 | 保留最近 7 天，且**独立于「这次有没有写新快照」** | 只跟着新快照走的话，用户数据一旦不再变化，早先的快照就永远堆着，「最多 7 份」的承诺不成立（这个问题是测试跑出来的，见下） |
| 文件夹句柄 | 存进 IndexedDB（`FileSystemDirectoryHandle` 可以结构化克隆）；文件夹名另存一个字符串 | 句柄能存下来就不用每次重选文件夹。名字单独存是因为克隆会丢掉在句柄上临时挂的属性，而设置页要显示名字 |
| 权限 | 句柄恒久，权限按会话过期，所以每次启动只 `queryPermission` 不 `requestPermission` | 申请权限必须由用户点击触发，否则浏览器直接拒绝。所以界面上是「需要重新授权」按钮，而不是自动弹窗 |

> ⚠️ **踩过的坑（两个都是测试跑出来的，不是看代码看出来的）：**
>
> 1. **导出的 .md 里图片是死链。** 拼相对路径时写了 `imageBase + path.split('/').pop()`，`pop()` 把 `images/` 前缀吃掉了，结果变成 `../../../x.png`。文件生成了、不报错、能打开——只有真去点图片才会发现是坏的。测试里加了一条「把相对路径解析回绝对路径，确认那个文件真在包里」才抓住。
> 2. **快照轮转不会执行。** 原本把清理写在「写新快照」的分支里，于是数据不再变化时永远不清理。测试造了 8 份历史快照，跑完还是 9 份。修法是把轮转提到分支外面——**清理是维护不变量，不该依赖于「正好这次有活干」。**

#### M6 实施记录（绘图，阶段 1）

需求是「在 app 里画机械原理图」。阶段 1 交付自由画板（直线/矩形/椭圆/手绘、网格吸附、撤销重做、选择拖动删除），能画受力图和矢量三角形，也能用直线圆拼出粗略的机构图。**规范化的机构运动简图要等阶段 2 的符号库。**

| 事项 | 决定 | 原因 |
| --- | --- | --- |
| 不用现成的白板库 | **手写 SVG，零新依赖** | Excalidraw 是手绘风格（基于 rough.js，故意让线抖动），正好和「规范的机构简图」相反，而且它没有机械符号库、场景格式也塞不进 `asset://` 管线。tldraw 的许可证要求生产环境配 license key、免费档强制水印，直接排除 |
| 场景存哪 | **内嵌在导出 SVG 的 `<metadata>` 里**，一个附件既是渲染结果又是源数据 | 不新建表 → 备份格式、孤儿清理、导入导出、`asset://` 引用**全部零改动**。建独立表则要动备份格式的三处复用实现，还会引入「附件和记录两条命」的漂移问题（删了图，记录悬挂；记录钉住图，图永远清不掉） |
| 画板放哪 | **NotePage 内的全屏浮层**，不是新路由 | 插入结果要落到**光标位置**，而正文的拥有者是 ProseMirror 实例。走独立路由会让 NotePage 卸载、编辑器销毁，插入只能绕道数据库，还要在画板里重写一套保存链路。浮层之下编辑器始终挂着，插入走新加的命令通道，之后的自动保存/孤儿清理/自动备份全部沿用既有链路 |
| 怎么重新打开已有的图 | header 的「插入图形」按钮 → 列出本篇的图形 → 点「编辑」 | 正文里的图是一行普通 Markdown 图片，Crepe 的图片块没有暴露点击回调。为「点图就能编辑」去写自定义 ProseMirror 节点视图，是这块功能里最不划算的投入 |
| 编辑已有的图 | **覆盖同一个附件**（`AttachmentRepository.replace`），不新建 | 新建再改正文意味着要动编辑器文档，还会让备份的镜像目录（只增不删）每编辑一次就多堆一个文件。覆盖要保持正文里的 `asset://<id>` 稳定 |
| 图形颜色 | **写死在 SVG 里**（白底 `#ffffff` + 描边 `#1f2937`），不跟随主题 | 正文里的图是 `<img src="blob:...">`，SVG 在 img 里是**独立文档，继承不到页面的 `currentColor`**，`text-neutral-800 dark:text-neutral-100` 那套对它完全无效。按制图惯例固定成白底黑线：浅色下完全正确，深色下是一张「白图纸」 |
| 主题图标 | 画板里继续用 Unicode 字符（`↖ ╱ ▭ ◯ ✎`） | 和项目里 `⌕ ◐ ⚙ ◎` 那套一致。阶段 2 的机构符号面板需要真正的矢量图形，那会是第一次引入内联 SVG 图标，届时要单独记一笔 |

> ⚠️ **这次踩到的三个坑，都是「看起来能跑但其实是坏的」：**
>
> 1. **`![说明](asset://id)` 里的说明文字会被静默吞掉。** Crepe 的 image-block 把 Markdown 的 **alt 槽位当作缩放比例**、**title 槽位才是说明文字**（`parseMarkdown: ratio = Number(node.alt || 1), caption = node.title`）。所以手写 `![四杆机构](url)` 会被解析成 `Number('四杆机构') = NaN → 1`，文字丢掉，而且不报错。正确写法是 `![1.00](url "四杆机构")`。**结论：插入必须直接构造 image-block 节点并设 attrs，不能拼 Markdown 字符串。**
> 2. **`Attachment.type` 会在「备份 → 恢复」一趟之后静默降级。** `lib/backup/format.ts` 导出时写的是真实值，但 `archive.ts` 导入时**写死了 `type: 'image'`**。导出正常、导入正常、数据库里看也正常，只是类型没了——正是本文件最怕的那类故障。已改成原样读回。
> 3. **覆盖附件之后光清缓存不够。** `lib/asset.ts` 按附件 id 缓存 blob URL，而编辑器里的 `<img>` **已经拿着旧地址了**，清缓存不会让它回头重新解析。结果是数据库里明明是新的、屏幕上还是旧的。解法是 `refreshAssetImages`：清缓存 + 把页面上仍指向旧地址的 `<img>` 直接换掉，最后才释放旧 URL。

#### M4 实施记录（键盘快捷键）

M4 原本是「深色模式打磨、键盘快捷键、性能优化」三件事，本次只做**快捷键**，其余两项另立 M4.5。除了补绑定，还解决了一个可发现性问题：应用里到底有哪些快捷键，用户原本无处可查——App 自己只实现了 4 处（`Ctrl+K`、`Esc`、画板的工具键、标题框 `Enter`），而用户真正会去找的那批（加粗、标题、公式）全部来自 Milkdown 自带的 keymap，界面上唯一的痕迹是 Crepe 工具栏的**英文** tooltip（项目没设 locale），在全中文界面里基本等于没有。

| 事项 | 决定 | 原因 |
| --- | --- | --- |
| 组合键写在哪 | **只写在目录里**（`src/lib/shortcuts/catalog.ts`）。绑定代码只携带条目 id，组合键从目录查 | 「一览表里写着 `Ctrl+/`、代码里绑的却是别的」是这类功能最容易出、也最难被发现的错。绑定按 id 查表，两边在数据上就是同一份；写错 id 直接是编译错误 |
| 修饰键匹配 | **精确匹配**：声明 `Ctrl+B` 就只认 `Ctrl+B` | 原来那句 `(ctrlKey \|\| metaKey) && key === 'k'` 让 `Ctrl+Shift+K`、`Ctrl+Alt+K` 也能打开搜索。多打开一次不算事，真正的代价是以后每加一个绑定都要回头检查它有没有被别的绑定顺手吃掉 |
| `Ctrl+B` 怎么同时是「加粗」和「折叠侧栏」 | 全局处理器的第一句是 `if (event.defaultPrevented) return` | ProseMirror 的 `handleKeyDown` 命中时会调 `preventDefault()`，而它的监听器挂在 contenteditable 上、也就是 `window` 的**上游**，所以这里能看见。编辑器先表态、全局层再兜底，不需要维护「焦点在不在编辑器里」这类判断。同理，代码块里的 CodeMirror、输入法的组合状态也靠这一条让位 |
| 浮层怎么让位 | 模块级**计数器**（`lib/shortcuts/overlay.ts`），不用查 DOM | 对话框开着时按 `Ctrl+K` 会把对话框连里面填了一半的字一起卸载掉，画板更严重（未保存的图）。不用 `querySelector('[aria-modal]')` 是因为**画板不是 dialog**、按属性查恰好会漏掉最需要挡的那个；而且查 DOM 会把快捷键层和标记结构绑死，以后改标记就静默失效 |
| 对话框的 `Esc` | 改挂 `window` 的**捕获**阶段 + `stopPropagation` | 画板在自己的容器上无条件 `stopPropagation` 挡全局键，而对话框是画板的子节点，冒泡到 body 就被拦住了——挂冒泡阶段的话，画板里**任何**新加的对话框按 `Esc` 都关不掉（原来只有「放弃这次改动？」能关，是靠一处特判） |
| 新建的键位 | `Alt+N`，不是文档里原来写的 `Ctrl+N` | `Ctrl+N` / `Ctrl+T` / `Ctrl+W` 是浏览器进程保留键，页面根本收不到，绑了等于没绑——而且是那种「自动化测试里能过、真人按下去没反应」的假成功。自检脚本里有一条专门守着这个：任何 App 绑定撞上保留键就直接失败 |
| 一览表放哪 | 设置页新增「键盘快捷键」一节，`Ctrl+/` 跳过去并高亮 1.6 秒 | 做成弹窗要新开一套浮层；做成侧栏入口要多一个路由，而内容只有一张表。代价是唯一入口落在二级页面里——用 `Ctrl+/` 直达来抵消，这也是「唯一的入口不能藏在深处」那次的教训 |
| 折叠侧栏要配可见入口 | 折叠态在 `<main>` 旁边渲染一条 32px 宽的窄栏按钮 | `toggleSidebar` 在这之前**零调用者**，折叠后侧栏整个卸载：忘了 `Ctrl+B` 就是一道单向门，得清浏览器数据才能回去。用占位窄条而不是 `absolute` 浮层，因为浮层会压在笔记标题输入框上 |

> ⚠️ **踩过的坑：**
>
> 1. **笔记 → 笔记切换不重建组件，会把上一篇的正文写进下一篇。** 从 `/notes/A` 到 `/notes/B`，React 认的是同一个路由元素，组件**不卸载**；`NotePage` 的初始化被 `initializedRef` 挡住、`NoteEditor` 的 `initialMarkdownRef` 又是挂载时取一次，于是打开 B 看到的还是 A 的正文，接着自动保存把它写进 B——**跨笔记写错数据**。这个问题一直存在，只是应用里原本没有「笔记 → 笔记」的链接所以碰不到，而 `Alt+N` 一上线就是这条路径。修法是按 `noteId` 给这一层加 `key`，让 React 按笔记身份重建；比逐条 reset 可靠，以后新加的状态自动是干净的。
> 2. **编辑器把正文交给保存层有最多 200ms 的延迟。** `@milkdown/plugin-listener` 的 `markdownUpdated` 自带 200ms 防抖，所以刚敲完立刻按 `Ctrl+S`，存下的是少了最后几个字的内容；1 秒后那次自动保存会把完整版本补上，**不丢数据**。这是给测试打桩时量出来的（同一段文字被写了两次：按下后 2ms 一次、1057ms 一次）。没有改：真人的手速到不了 200ms 以内，而改它要动编辑器的监听链路。这也是为什么自检脚本里打完字要停 300ms 再按 `Ctrl+S`——否则量到的是这 200ms，不是 `Ctrl+S`。
> 3. **`stopPropagation` 不阻止同一节点上的其它监听器。** 对话框的 `Esc` 用 `stopPropagation` 挡住了画板，但两个对话框同时开着还是会一起关（那是 `stopImmediatePropagation` 的语义）。应用里目前没有叠加对话框的场景，写进注释备查。
>
> 顺带记一条**测试纪律**：CDP 注入的按键走渲染进程通道，**绕过了浏览器进程的快捷键处理**。所以浏览器保留键在测试里「能过」、在真人手里没用；中文输入法选字时 `key` 会变成 `'Process'`，也测不到。这类断言只能写成数据检查 + 人工验证，硬写行为断言只会得到永远通过的假测试。

#### M6.5 实施记录（机构符号、吸附、机构语义、自定义符号、橡皮）

M6 交付的自由画板能画受力图和矢量三角形，但**画不出规范的机构运动简图**——四杆机构的铰链要一个个手摆，摆不准也拖不动，而这正是这个功能存在的理由。M6.5 补齐这块：左侧符号面板（标准 10 个 + 自己画的）、端点吸附、Shift 角度约束、拖动铰链联动构件、自定义符号、橡皮。

| 事项 | 决定 | 原因 |
| --- | --- | --- |
| 符号怎么表示 | 加两种图形：`symbol`（点符号，定尺寸可旋转）和 `link`（两点符号，长度由两端点决定） | 两者的**几何坐标系根本不同**：点符号是「平移 + 旋转」的定尺寸局部系，两点符号的局部 +x 轴沿 A→B 且长度就是 \|AB\|。合成一个（带可空 `b`）就会得到一个「`b` 为 null 时语义全变」的字段 |
| 定义写成数据还是函数 | **点符号是纯数据、内联进场景；两点符号是代码里的生成器** | 场景 JSON 要塞进 `<metadata>`，`JSON.stringify` 会把函数丢掉，所以能内联的必须是纯数据。两点符号的图元长度依赖 \|AB\|，写成数据就要给每个坐标标「这一维乘长度」——而**哪些量跟长度变是不一致的**（带传动的轮半径固定、构件的铰链圆半径固定），那个混合坐标系是出 bug 的地方 |
| 不用 SVG 的 `scale()` | 两点符号的零件按**实际长度**生成 | `scale` 会把 `stroke-width` 一起放大，要抵消就得用 `vector-effect="non-scaling-stroke"`，那是又一个会静默失效的开关 |
| 渲染路径 | 新增 `render.tsx`：`shapeToParts` 是**唯一**把图形翻译成几何的地方，画布和导出各自只做「零件 → React 元素 / DOM 节点」 | 原来两边是一对手工维护的双胞胎（`ShapeView` / `createShapeElement`），各算一遍几何。加两种图形只会有三处，而**漏改是静默的**：React 那边少个东西只是看不见，导出那边被 `if (el)` 跳过则是「正文里的图缺一块、metadata 里却完好」 |
| 机构语义 | **端点吸附 + 重合端点联动**，不引入铰链/构件实体、不做约束求解 | 「拖动铰链联动构件」不需要知道谁铰接在谁身上：吸附保证「铰链」在数据上**真的是同一个坐标**，联动保证重合的点永远一起走。§1.4 明确排除参数化求解，这是唯一不越界的做法 |
| 固定构件为什么拒绝拖动 | 待移动的点里只要有一个机架类锚点，**整次拖动被拒绝**并给出提示 | 不做约束求解不等于机架可以乱动。拒绝比「允许但不动」诚实：后者会静默把重合拉断，用户看到的是机构散了却找不到是哪一步弄散的 |
| 重合判定用容差 | 1 个图纸单位，**不能用 `===`** | 拖拽走的是屏幕坐标换算回来的原始点，拖走再拖回不可能落在严格同一个数值上。容差取 1 而不是 6：太大了会把两个挨得近但不同的铰链并成一个 |
| 自定义符号存哪 | **一张正经的表**（Dexie v2）+ 进备份 | 这是用户创作的内容，不能塞进 `settings`——那张表刻意不进备份，恢复一次就没了 |
| 备份格式升到 v2 | 宁可让旧版 App **报错**，也不让它静默丢掉整个符号库 | 版本号只被当作上界用。不升的话旧版导入新备份会无声丢掉用户画的符号；报错至少是可见的 |
| 场景格式版本**按内容写** | 没有符号的图写 1，有符号的写 2 | 旧版画板读到高于自己上限的版本会拒开。无条件升到 2 的话，随手画的两条直线回滚后就打不开了；按能力写则只有真的用了符号的打不开——而那本来就该打不开 |
| 自定义符号只支持点符号 | 定尺寸、可旋转、点一下放置 | 两点符号的几何是代码里的生成器，不是数据，没法存进库。要不同尺寸就存两个 |
| 符号里不能放符号 | 编辑模式的工具栏里**没有符号面板** | 从构造上排除递归，比加一层运行期校验可靠 |
| 橡皮是整笔擦除 | 一次拖动扫过的图元整块删掉，整次拖动记**一步**历史 | 按「每擦掉一个提交一次」写的话，撤销一次只回来一个图元，等于撤销坏了 |
| `ToolKind` 不再派生自 `ShapeKind` | 改成显式联合，并加 `Tool` 判别联合（放符号要带一个 ref） | 图形种类和工具不再一一对应（「放置符号」是一种工具、对应两种图形），派生关系一旦不成立，留着它只会让人以为加一种图形就自动多一个工具 |
| 缩略图 | 面板图标、画布、导出**走同一份 `Part`** | 图标不可能和放下去的东西长得不一样。这是这个项目第一次用真正的矢量图形当图标（此前都是 `⌥ ◐ ⚙ ◎` 那类 Unicode 字符）——`tools.ts` 里当初留的预告在这里兑现 |

> ⚠️ **踩过的坑：**
>
> 1. **`noImplicitReturns` 抓不到「有 `default` 分支」的 switch。** 加两种图形之前先做了一次实测：往 `Shape` 联合里塞一个假种类，看编译器在哪几处报错。结果是 8 处会报（`isDegenerate`、`ShapeView`、`hitsShape`、`shapeBounds`、`translateShape`、`createShapeElement`、草稿推进的 `{...draft, b}`、`TOOL_ICONS`），**漏掉两处**：工具→构造器那条三元链（新工具会被静默当成手绘）、以及 `serialize.ts` 里的 `SHAPE_KINDS`（`Set<string>`，漏改会让新图存得出去却读不回来）。后者改成了映射类型，加了 kind 不写校验器就是编译错误。
> 2. **`translateShape` 的 `default` 分支会去读 `shape.a`，而点符号没有 `a`。** 那是**拖拽主路径**，不是边缘情况——`move(undefined)` 直接 TypeError。光加穷尽性守卫不够，那个 `default` 本身就是吞掉新种类的地方，必须拆成显式 case。
> 3. **既有的静默数据丢失：附件读不出来时点「完成」会把它覆盖成一张白纸。** `readSceneFromBlob` 失败只 `setError`，而 `handleSave` 完全不看 `error`，照样把当前（空）场景写回去。以前要手工改坏 SVG 才能碰到；**场景格式升到 2 之后，一个还开着旧版 JS 的标签页**（PWA 更新后旧标签页不会自刷新）就能走到这条路。修法是读失败时连同「完成」一起禁掉，并且不渲染画布。
> 4. **自检脚本会被上一次没清干净的 dev 服务器骗。** `--strictPort` 会让新服务器在端口被占时退出，而脚本「等端口有响应」却被**旧进程**满足了，于是整个脚本测的是那份旧代码，全程不报错。表现是：一条断言一直失败，而代码本身是好的（这次真的去改了没坏的东西）。修法有两条：跑之前先检查端口是不是已经有人应答、直接报错；以及 teardown 里 `await` taskkill 真正退完再 `process.exit`。
> 5. **改代码的脚本本身会静默失效。** 用批处理脚本改 `contentHashOf` 时字符串没匹配上，替换静默什么都没做——**而自检脚本当场把「改了符号但内容指纹不变」报了出来**。这正是那条断言存在的意义：指纹不变的话，镜像备份会判定「没变化」直接跳过整轮写入，当天的每日快照也不写，符号会一直不进备份且毫无提示。
> 6. **点符号的缩略图一度是一条看不见的线。** SVG 的 `viewBox` 会把线宽一起缩放：一个 200 单位宽的定义塞进 32px 的方框，3 单位的线宽只剩 0.5px。加了个 `widthScale` 按缩放比把线宽乘回来，「粗杆细圆」的粗细对比在图标上才成立。


---

## 1. 产品定位

### 1.1 一句话定位

一个**按科目分区、按章节归档**的个人课堂笔记工具，支持图文混排录入，在 Windows 电脑上本地运行，数据完全由自己掌控。

### 1.2 目标用户

- 主要用户：**本人**（单用户产品，无需注册、无社交、无协作）
- 使用设备：**Windows 电脑**
- 使用场景：
  - 课后整理、补充、排版课堂笔记
  - 边看课件/教材边录入要点，需要频繁截图粘贴
  - 复习时按科目/章节翻阅、检索

### 1.3 核心痛点与解法

| 痛点 | 本产品的解法 |
| --- | --- |
| 笔记散落在各种 App / 纸质本子里，找不回来 | 科目 → 章节 → 笔记 三级固定结构，一处存放 |
| 截图、课件图片没地方放，和文字对不上 | 图片直接内联到笔记正文里，图文一体 |
| 复习时翻不动，找不到某个知识点 | 全文搜索 + 章节浏览 |
| 笔记存在别人的服务器上，不放心 | 纯本地运行，数据不离开这台电脑 |

### 1.4 明确不做（Non-Goals）

划清边界，避免范围蔓延：

- ❌ 多人协作、分享、社区（单用户自用）
- ❌ 多端同步（已明确不需要）
- ❌ 移动端适配（Windows 桌面为主，不做手机体验优化）
- ❌ 手写笔迹识别（OCR）
- ❌ 完整的 Anki 式间隔重复复习算法
- ❌ 思维导图（**暂缓**，见附录 A）

绘图功能（M6）自己的边界，同样是刻意不做的：

- ❌ **运动学仿真**——不算连杆曲线、不解速度/加速度多边形，画的是静态图
- ❌ **参数化求解**——改一个杆长，整个机构**不会**自动重算位置。「改参数重画」在本项目里的准确含义是**「图能重新打开继续编辑」**（存的是铰链和构件的结构化数据，不是像素），不是约束求解
- ❌ 完整的 GB/T 4460 线型规范（点划线间隔、粗细线比例的全套规定、剖面线间距）——M6.5 已交付 10 个符号的图形特征和「杆用两倍粗实线」这一条，其余线型规范仍未实现
- ❌ **符号的图面准确性**——符号按标准的**图形特征**手绘，不是标准原图的复制件；齿轮副的节圆一律等径（没有齿数/传动比参数）。用途是课堂笔记里的示意图，**不作为制图依据**
- ❌ 自定义符号的缩放与两点形态——自定义符号只支持定尺寸的点符号（两点符号的几何是代码里的生成器，不是数据）
- ❌ 手绘笔迹的分段擦除——橡皮是整笔擦除
- ❌ 导出 DWG/DXF；也不做触屏手势（本就不做移动端）
- ❌ 在画板里插入截图当底图（想「描着课件画」不行）

---

## 2. 信息架构

### 2.1 三级结构

```
科目 Subject（如：高等数学）
 ├── 章节 Chapter（如：第三章 微分中值定理）
 │    ├── 笔记 Note（如：2026-09-15 罗尔定理课堂记录）
 │    ├── 笔记 Note
 │    └── ...
 ├── 章节 Chapter
 │    └── ...
 └── ...

科目 Subject（如：数据结构）
 └── ...
```

### 2.2 层级规则

- **科目**：顶层容器，数量少（几门到十几门）。有颜色标识，便于视觉区分。
- **章节**：隶属于唯一科目，可手动拖拽排序。
- **笔记**：隶属于唯一章节，是实际的内容载体。按创建时间倒序排列，可置顶。

### 2.3 关键设计决策：章节不嵌套，层级交给正文标题

不在 UI 上做无限级目录树，而是在**笔记正文里用标题层级表达粒度**（Markdown 的 `##` `###`，或富文本的 H2/H3）。

理由：

1. 录入时不用先想好"这属于哪个子目录"，降低记录摩擦；
2. 正文标题天然就是知识结构的骨架，将来要做思维导图时可直接解析，无需额外设计；
3. 想要更多层级时，用户写 H4 即可，零成本扩展。

---

## 3. 功能需求

优先级说明：**P0 = MVP 必须**，**P1 = 第二版**，**P2 = 远期/可选**。

### F1. 科目管理（P0）

| 编号 | 需求 | 优先级 |
| --- | --- | --- |
| F1.1 | 新建科目（名称 + 颜色 + 可选描述） | P0 |
| F1.2 | 重命名 / 删除科目（删除需二次确认，提示将连带删除的笔记数） | P0 |
| F1.3 | 科目列表页：显示科目名、章节数、笔记数、最近更新时间 | P0 |
| F1.4 | 科目排序（拖拽） | P1 |
| F1.5 | 归档科目（不删除但移出主列表） | P2 |

### F2. 章节管理（P0）

| 编号 | 需求 | 优先级 |
| --- | --- | --- |
| F2.1 | 在科目下新建章节（名称 + 可选序号） | P0 |
| F2.2 | 重命名 / 删除章节 | P0 |
| F2.3 | 章节列表页：显示笔记数、最近更新时间 | P0 |
| F2.4 | 章节排序（拖拽） | P1 |

### F3. 笔记编辑（P0）

| 编号 | 需求 | 优先级 |
| --- | --- | --- |
| F3.1 | 新建笔记，标题（默认取首行或自动生成日期） | P0 |
| F3.2 | 正文编辑，支持：加粗、斜体、标题层级、有序/无序列表、引用、分割线 | P0 |
| F3.3 | **图片插入**：粘贴剪贴板（Ctrl+V，桌面高频）、拖拽文件、点击选择文件 | P0 |
| F3.4 | 图片在正文中**内联显示**，可点击放大预览、可删除 | P0 |
| F3.5 | 自动保存（停止输入 1 秒后保存，界面显示"已保存"状态） | P0 |
| F3.6 | 撤销 / 重做（Ctrl+Z / Ctrl+Y） | P0 |
| F3.7 | 常用键盘快捷键，且**应用内可查**（设置页的「键盘快捷键」一节，`Ctrl+/` 直达）。`Ctrl+F` **不接管**，留给浏览器的页内查找——全局找东西已经有 `Ctrl+K`，而在一篇长笔记里用原生查找是有用的 | P1 |
| F3.8 | 表格、代码块、数学公式（KaTeX） | P2 |
| F3.9 | 导入 Word/网页内容自动转格式 | P2 |
| F3.10 | **绘图**：在笔记里画机构简图/示意图，插到光标处；已有的图能重新打开编辑（见 M6） | P1 |

**绘图的边界（详见 §1.4 Non-Goals）：** 这是「笔记里的示意图」，不是 CAD 出图。不做运动学仿真，也不做参数化求解——「改参数重画」的准确含义是「图能重新打开继续编辑」，不是「改一个杆长整个机构自动重算位置」（那需要约束求解器，是另一个量级）。

**图片处理要求（Windows 桌面场景）：**

- **粘贴优先**：Windows 上最常用的操作是 `Win+Shift+S` 截图后 `Ctrl+V` 粘贴，这条路径必须最顺畅；
- 上传时自动压缩（长边限制在 1600px 内，JPEG 质量 80%），避免课件大图撑爆存储；
- 支持一次粘贴/拖入多张图片，按顺序依次插入；
- 图片以二进制形式存在本地（见 §7.3），**不依赖任何外部图床**。

### F4. 浏览与检索（P0）

| 编号 | 需求 | 优先级 |
| --- | --- | --- |
| F4.1 | 全局搜索：按笔记标题 + 正文关键词 | P0 |
| F4.2 | 搜索结果高亮命中片段，显示所属科目/章节 | P0 |
| F4.3 | 按科目 / 章节筛选 | P1 |
| F4.4 | 标签系统（笔记可打多个自定义标签，如 #重点 #考点 #不懂） | P1 |
| F4.5 | 笔记置顶 / 收藏 | P1 |
| F4.6 | 最近编辑列表（快速回到刚才在写的东西） | P1 |

### F5. 数据导出与备份（P0）

> ⚠️ **这是本项目最重要的一组功能，优先级与编辑功能同级。**
> 方案 A 是纯本地存储，浏览器的站点数据可能被系统清理、被误操作清空。**必须靠导出兜底，否则几年的笔记可能一夜消失。**

| 编号 | 需求 | 优先级 |
| --- | --- | --- |
| F5.1 | 一键导出全部数据（含图片）为压缩包 / JSON | P0 |
| F5.2 | 从导出文件完整恢复 | P0 |
| F5.3 | **自动备份到指定本地文件夹**（利用 File System Access API，可指向 OneDrive 同步目录，相当于免开发获得云备份） | P0 |
| F5.4 | 单篇笔记导出为 Markdown / PDF | P1 |
| F5.5 | 导出内容需为**开放格式**，脱离本 App 仍可阅读 | P1 |

### F6. 思维导图（暂缓）

完整设计见**附录 A**。当前不排期，待其他功能稳定后重新评估。

---

## 4. 核心数据模型

与具体技术栈无关的概念模型：

```
Subject（科目）
  id            string   主键
  name          string   科目名
  color         string   十六进制色值
  order         number   排序位
  createdAt     datetime
  updatedAt     datetime

Chapter（章节）
  id            string   主键
  subjectId     string   外键 → Subject
  name          string   章节名
  order         number   排序位
  createdAt     datetime
  updatedAt     datetime

Note（笔记）
  id            string   主键
  chapterId     string   外键 → Chapter
  title         string   标题
  content       text     正文（存储格式取决于 §5 的决策）
  contentFormat enum     'markdown' | 'richtext_json'
  excerpt       text     纯文本摘要（用于列表预览与搜索，由正文自动生成）
  isPinned      boolean  是否置顶
  tags          string[] 标签（P1）
  createdAt     datetime
  updatedAt     datetime

Attachment（附件 / 图片）
  id            string   主键
  noteId        string   外键 → Note
  type          enum     'image'
  blob          binary   图片二进制数据（或存储引用）
  mimeType      string
  width         number
  height        number
  sizeBytes     number
  createdAt     datetime

Setting（设置）
  key           string
  value         json
```

> **说明：** 没有 `MindMap` 表，因为导图功能暂缓（附录 A 中保留了该设计，启用时再加）。

---

## 5. 【重点】笔记正文：Markdown 与富文本的区别

这是目前唯一未定的技术决策（D5），直接决定录入体验和数据结构，所以单独展开讲清楚。

### 5.1 一句话概括

| | Markdown | 富文本（所见即所得） |
| --- | --- | --- |
| **核心特征** | 你用**符号**写格式，如 `**粗体**`、`## 标题` | 你**选中文字点按钮**，界面直接显示最终效果 |
| **文件的本质** | **纯文本**。用记事本打开就是 `## 第三章\n- 罗尔定理` | **结构化数据**。底层是 HTML 或 JSON 节点树 |
| **类比** | 写代码 | 用 Word |

### 5.2 具体操作对比

假设你要写一行加粗的标题：

**Markdown 的写法**——手动敲符号：

```
## 微分中值定理
其中 **罗尔定理** 是最基础的一个。
```

**富文本的写法**——选中文字，点工具栏的 `B` 和"标题2"按钮，界面上看到的直接就是**加粗的大标题**。

### 5.3 五个关键差异（结合本项目）

#### 差异 1：数据可移植性 ★★★ 最重要

- **Markdown**：文件本身就是纯文本。
  - 导出就是 `.md` 文件，**用记事本、VS Code、任何编辑器都能打开**；
  - 10 年后这个 App 没了，你的笔记依然完整可读；
  - 可以直接丢进 Git 做版本管理，可以全文 grep 搜索，可以用 Obsidian/Typora 打开继续用；
  - 备份就是复制文本，体积极小。
- **富文本**：文件是编辑器私有的 JSON 结构。
  - 存的是类似 `{"type":"heading","attrs":{"level":2},"content":[{"type":"text","text":"微分中值定理","marks":[{"type":"bold"}]}]}` 这样的东西；
  - **人能读，但不方便读**；脱离本 App 基本没法直接用；
  - 导出成 Markdown 需要写转换代码，转换过程有信息损耗；
  - 如果把编辑器从 Tiptap 换成别的，可能要写迁移脚本。

> **对一个"要存几年学习资料"的工具来说，这一条权重最高。** 笔记的价值在于长期留存，而数据的长期可读性正是 Markdown 的最大优势。

#### 差异 2：录入速度与专注度

- **Markdown**：**手不离键盘**。写 `**重点**` 不需要右手离开主键区去点工具栏，思路不中断。对"边听课边快速记录"效率更高，熟练后比富文本快。
- **富文本**：需要鼠标/触控板配合点按钮，或记快捷键。**零学习成本**，但选格式时会打断输入节奏。

#### 差异 3：学习成本

- **Markdown**：需要花 10 分钟记住大约 8 个符号（`#` `**` `*` `-` `1.` `>` `` ` `` `[]()`）。对没接触过的人，初期会一边写一边查语法，有摩擦。
- **富文本**：**零成本**，会用 Word 就会用。

#### 差异 4：粘贴外部内容

- **富文本**：从网页、Word、课件里复制一段带格式的内容，粘贴进来**格式基本保留**（标题、加粗、颜色）。这是它的强项。
- **Markdown**：粘贴带格式的内容时，通常只能拿到纯文本，需要重新加标记。**但这个缺点在本项目里影响有限**——课堂笔记的内容主要是自己敲的要点 + 截图，很少整段搬运排版复杂的网页。

#### 差异 5：稳定性与不可见字符

- **Markdown**：纯文本，**永远不会出现"格式错乱"**。所见即所得编辑器偶尔会有光标跳动、列表缩进乱掉、粘贴后样式异常等问题。
- **富文本**：复杂编辑操作（跨段删除、列表嵌套）偶尔会有诡异的边界 bug，这是所有富文本编辑器的通病。

### 5.4 结论与建议 ★

**建议采用「Markdown 作为存储格式 + 即时渲染作为编辑体验」的组合。**

这是两者的**最优交集**，市面上如 Typora、Obsidian 的实时预览模式就是这种形态：

- 你输入 `## 微分中值定理` 并按下空格 → **这一行当场就变成大标题**，标记符号自动隐藏；
- 你输入 `**罗尔定理**` → **立刻就地加粗**；
- 所见即所得，**学习成本≈0**，体验上感觉不到自己"在写 Markdown"；
- 但**底层存的仍然是纯 Markdown 文本**——保住差异 1 的全部好处。

这样一来，5.3 里 Markdown 的主要缺点（学习成本、不直观）基本被消解，而它的核心优点（可移植、可迁移、抗格式错乱）全部保留。

**✅ 已确定采用 Milkdown**（决策 D5，2026-09-19）。技术落点对比见下表，供后续回顾：

| 方案 | 说明 | 结论 |
| --- | --- | --- |
| **Milkdown** | 基于 ProseMirror + remark，**原生就是"Markdown 即时渲染编辑器"**，开箱即得上述体验，文档质量好 | ✅ **已选用** |
| Tiptap + Markdown 扩展 | 生态最大、可定制性最强，但即时渲染的输入规则需自己配置调优 | 备选 |
| 普通 textarea + 双栏预览 | 实现最简单（半天搞定），但没有即时渲染，体验差一档 | 未采用 |
| Tiptap 纯富文本，存 JSON | 体验最接近 Word，但放弃数据可移植性，与"长期留存学习资料"的目标冲突 | 未采用 |

**选 Milkdown 的核心理由：** 它是唯一一个**开箱即得"即时渲染 + 纯 Markdown 存储"**的成熟方案，不需要为这个体验额外付开发成本。Tiptap 虽然生态更大，但要达到同样的即时渲染效果需要自己逐条配置输入规则，属于用开发时间换生态——对本项目的规模不划算。

> **兜底原则：** 无论选用哪种，**导出为 Markdown 的能力（F5.1/F5.4）都必须在 M3 阶段做扎实**。有 Markdown 导出，格式转换就是可逆的；没有，就可能被锁死。

### 5.5 对图片的处理差异（实现细节）

这一点影响开发工作量，需要顺带说明：

- **Markdown 模式**：图片在文本里就是一个引用标记。本项目的做法是：粘贴图片 → 存入 IndexedDB 得到 id → 在正文里写 `![图片](asset://<id>)` → 渲染时把 `asset://<id>` 替换成实际的图片地址。
  - 优点：正文保持纯文本，导出时图片可以一起打包；
  - 需要写一点自定义解析逻辑（约半天工作量）。
- **富文本模式**：编辑器自带"图片节点"类型，插入图片是内置能力，不需要自定义语法。
  - 优点：省事；
  - 缺点：图片位置信息被埋在 JSON 结构里，导出时要专门遍历提取。

**两种模式的图片都是存本地二进制，不依赖外部图床**——这点完全相同。

### 5.6 后续约束

既然已确定用 Markdown 存储，以下两件事需要在开发中守住：

1. **导出为 Markdown 的能力（F5.1/F5.4）必须在 M3 阶段做扎实。** 有 Markdown 导出，将来换编辑器/换 App 的格式转换就是可逆的；没有，就可能被锁死。
2. **编辑器的"即时渲染"只是显示层。** 数据库里存的必须是 Markdown 原文（`note.contentFormat = 'markdown'`），不能图省事存成 ProseMirror 的内部文档结构——否则 §5.3 里差异 1（数据可移植性）的全部好处就白拿了。

---

## 6. 关键交互流程

### 6.1 记录一篇笔记

```
打开 App（PWA 独立窗口）
  → 选择科目（大卡片，一眼识别）
  → 选择章节
  → 点「+ 新建笔记」
  → 输入标题（可跳过，自动用日期）
  → 正文编辑区
      ├ 键盘输入文字，格式符号即时渲染
      ├ Win+Shift+S 截图 → Ctrl+V 粘贴 → 图片插入光标处
      ├ 拖拽图片文件到编辑区 → 依次插入
      └ 点击图片 → 放大预览 / 删除
  → 停止输入 1 秒 → 自动保存 → 顶部显示「已保存」
```

**优化点：** 首次打开 App 直接落在"上次编辑的笔记"，而不是科目列表。再加一个 `Alt+N` 全局快捷键，当前章节下一键新建笔记。（原本写的是 `Ctrl+N`，实现时改成 `Alt+N`：`Ctrl+N` 是浏览器进程保留键，页面收不到。）

### 6.2 复习

```
搜索框输入关键词（Ctrl+K）
  → 跨科目全文检索
  → 命中片段高亮，显示所属科目/章节
  → 点击跳转到笔记

或

科目页 → 章节页 → 笔记列表（按时间倒序）→ 打开笔记
```

---

## 7. 技术栈（方案 A：本地优先的 PWA）

### 7.1 选型组合

| 层 | 选型 | 说明 |
| --- | --- | --- |
| 构建工具 | **Vite** | 启动快、配置简单，适合纯前端项目 |
| 框架 | **React 19 + TypeScript** | 生态最好，编辑器库支持最全 |
| 样式 | **Tailwind CSS** | 快速出 UI，与组件库配合好 |
| UI 组件 | **shadcn/ui** | 复制源码到项目里，可自由改，无依赖锁定 |
| 编辑器 | **Milkdown** ✅ | Markdown 即时渲染，存储为纯 Markdown，见 §5.4 |
| 本地数据库 | **Dexie.js**（IndexedDB 封装） | 存科目/章节/笔记/图片，API 友好 |
| 状态管理 | Zustand | 轻量，够用 |
| 路由 | React Router | — |
| 备份 | **File System Access API** | Chrome/Edge 支持，可直接写入本地文件夹 |
| 打包 | **fflate** | 约 8KB，比 jszip 轻得多；用它的流式接口打包，能报进度、内存有界 |
| 离线 / 安装 | **手写 Service Worker + Web App Manifest** | 不引入 Workbox：应用完全静态、资源集固定，预缓存逻辑约 80 行，自己写更可控。见 §7.5 |
| 静态服务 | **自写的 `scripts/serve.mjs`** | 零依赖。不用 `vite preview` 是因为它端口不固定、且对任何 404 都回 index.html。见 §7.2 |
| 部署 | **本地运行**，固定端口 `127.0.0.1:24816` | 不部署到 Vercel——本地固定端口同样给出稳定 origin，且数据不出这台机器。见 §7.2 |

### 7.2 在 Windows 上怎么"运行"这个 App

这是方案 A 的常见疑问，说明清楚。有三种方式，按体验排序：

| 方式 | 做法 | 体验 | 适用阶段 |
| --- | --- | --- | --- |
| **① PWA 安装**（推荐） | 在 Edge 或 Chrome 里访问地址 → 点地址栏的"安装"图标 → 变成独立窗口应用 | ⭐⭐⭐ **有独立窗口、开始菜单/任务栏图标、无浏览器地址栏，看起来就是一个本地软件**；离线完全可用 | 开发完成后日常使用 |
| ② 浏览器标签页 | 直接访问地址，收藏为书签 | ⭐⭐ 能用，但混在浏览器标签里，容易关掉 | 开发调试期 |
| ③ 纯本地文件 | 构建后得到静态文件，双击 `index.html` | ⭐ 受浏览器本地文件安全策略限制，IndexedDB 可能受限，**不推荐** | 不推荐 |

> **关键点：** PWA 装在 Windows 上**不是"网页"，而是一个独立的桌面应用窗口**。它有自己的图标、能固定到任务栏、能用 `Alt+Tab` 切换。代价是它仍然是浏览器引擎驱动的（这在本项目里是优势而不是劣势——图片渲染、文本编辑这些正是浏览器最擅长的）。

**实际落地的做法（M3.5）：**

- **开发**：`npm run dev` 起服务，热更新即时生效。开发服务器上**不注册** Service Worker——它会缓存 vite 的开发产物，导致改代码看不到效果。
- **日常**：`npm run build` 后用 `npm run serve` 打开 `http://127.0.0.1:24816/`，在 Edge 里点「安装」。
- **装完之后不需要开着服务器**：Service Worker 把整个应用（148 个文件、约 3.5MB）缓存下来，双击任务栏图标就能打开。只有「重新构建后要更新」和「缓存被清掉后要恢复」这两种情况才需要再起一次 `npm run serve`。

> **⚠️ `http://127.0.0.1:24816/` 这个地址是永久的，不要改。** 它同时决定了四件事：IndexedDB 里的笔记在哪、CacheStorage 里的离线缓存在哪、装出来的桌面应用是哪一个、任务栏上那个图标是谁。换端口不会迁移任何东西——你会得到一个空的应用、第二个图标，而笔记还留在旧端口下。这就是 `scripts/serve.mjs` 遇到端口被占用时**直接报错退出、绝不自动换端口**的原因。

> **不部署到 Vercel 了。** 原计划里提到「可选部署到 Vercel 获得固定 URL」，现在不需要：本地固定端口同样给出了稳定 origin，而且数据不出这台机器，符合 §1.4「不做多端同步」的定位。

### 7.3 数据存储策略

```
IndexedDB（Dexie.js）
 ├── subjects      科目
 ├── chapters      章节
 ├── notes         笔记（正文文本）
 ├── attachments   图片（Blob 二进制）
 └── settings      设置
```

**关键技术点：**

1. **图片存 Blob 而非 base64。** base64 会让体积膨胀约 33%，且大字符串读写慢。IndexedDB 原生支持 Blob 存储。
2. **列表页不加载全文本。** `notes` 表存 `excerpt`（纯文本摘要，前 100 字），列表页只读摘要字段，避免几千篇笔记时卡顿。搜索时再按需读取正文。
3. **写操作必须防抖。** 自动保存用 1 秒防抖，避免每敲一个字就写一次数据库。
4. **数据访问层抽象。** 所有数据库操作封装在 `repository/` 目录下（如 `NoteRepository.ts`），页面组件**不直接调用 Dexie API**。这样将来若要换存储方案，只改一处。

### 7.4 数据安全方案（重要）

方案 A 最大的风险是**浏览器站点数据被清理导致笔记丢失**。用三层防护化解：

| 层级 | 措施 | 效果 |
| --- | --- | --- |
| 第 1 层 | 请求持久化存储权限（`navigator.storage.persist()`） | 告诉浏览器"这是重要数据，清理时请跳过"，可显著降低被自动清理的概率 |
| 第 2 层 | **自动备份到本地文件夹**（File System Access API） | 用户指定一个文件夹（如 `D:\笔记备份\` 或 OneDrive 目录），App 定期把数据写入为普通文件。**这是最实在的保障** |
| 第 3 层 | 手动一键导出（F5.1） | 关键时刻的完整兜底 |

> 第 2 层是方案 A 在**桌面端**相比移动端的巨大优势：Windows 上的 Chrome/Edge 完整支持 File System Access API，可以直接读写本地文件夹。指向 OneDrive/坚果云目录，就等于**免费获得了异地云备份**（F5.3）。这个能力在手机浏览器上是不具备的。

**M3 已全部落地**，入口在侧栏「设置与备份」：

- 第 1 层在 `AppLayout` 启动时自动请求（`src/db/index.ts` 的 `requestPersistentStorage`）。浏览器可能不批（比如站点使用时间太短），所以设置页会如实显示「已启用 / 未启用」，并说明这一层不可依赖。
- 第 2 层写在用户选的文件夹里：`data.json` + `笔记/科目/章节/标题.md` + `images/`（镜像）+ `快照/XXBJ-日期.zip`（每日快照，保留 7 天）。
- 第 3 层是设置页的「导出为 zip 文件」。

> **注意：权限是按会话过期的。** 重新打开 App 后，句柄还在（存在 IndexedDB 里），但读写权限会退回 `prompt` 状态，需要用户点一下「重新授权」。这是浏览器的安全模型，绕不过去——所以启动时只**查询**权限，绝不自动弹窗申请（申请必须由用户点击触发，否则会被直接拒绝）。

> **顺带的好处（M3.5 起）：** 第 1 层的 `navigator.storage.persist()` 保护的不只是 IndexedDB，也包含 Service Worker 的 CacheStorage。所以「离线缓存被浏览器自动清理」这件事和第 1 层是同一个保护。它挡不住的是用户主动「清除浏览数据」——那种情况下 IndexedDB、CacheStorage 和 SW 注册会被一起带走。

### 7.5 离线与安装（M3.5）

**目标：装好之后双击任务栏图标就能打开，不需要任何后台进程。**

实现是手写的 Service Worker（`scripts/sw.template.js` → 构建时生成 `dist/sw.js`），没有引入 Workbox。对于一个完全静态、无后端、资源集固定的应用，预缓存逻辑大约 80 行，自己写反而更可控。

| 决定 | 原因 |
| --- | --- |
| 预缓存清单在构建后生成（`scripts/build-sw.mjs`） | 资源文件名带内容哈希，清单必须是构建产物，不能手写 |
| 缓存名 = 清单全部文件内容的哈希 | 取内容而非 mtime，让「没改代码的重新构建」不产生无谓的缓存翻新 |
| 预缓存跳过 `.woff` / `.ttf` | 每个 `@font-face` 的 `src` 都把 `woff2` 列在第一位，Edge 支持 woff2，后两个格式永远不会被请求。省 40 个文件 / 798KB |
| 继续预缓存 1.1MB 的 Milkdown 分片 | 运行时缓存只在「服务器开着时打开过笔记」才填充，而那正是不需要它的时候。要防的场景是「服务器关着，第一次点开笔记」，运行时缓存对它零覆盖 |
| `install` 分核心/非核心两级 | 核心（`index.html` + 所有 JS/CSS 分片）缺一就整个装失败——宁可没有离线能力，也不要一个「能启动、点进笔记就崩」的壳子 |
| **`install` 不调 `skipWaiting()`，`activate` 不调 `clients.claim()`** | 见下 |
| `fetch` 里先判协议、后判 origin | `blob:` URL 的 origin 就是页面自己的 origin，只判 origin 会拦下笔记里的图片，而 CacheStorage 只接受 http(s)，会直接抛 |
| 资源请求 404 时不回退到 `index.html` | 模块脚本收到 HTML 报 `Unexpected token '<'`，把「文件缺失」伪装成语法错误 |

**为什么延迟 `skipWaiting` 是要害：** 用户开着应用时我们重新构建，Vite 会删掉旧的哈希分片。如果新 SW 立刻接管并清掉旧缓存，运行中的页面内存里还是旧模块图，它懒加载 `NotePage` 时请求的是**新**哈希文件——那些文件已经不在磁盘上，旧缓存也没了，路由会静默挂掉。延迟之后，新 SW 停在 waiting，两个缓存并存，旧页面继续用旧缓存服务；用户点「更新」才切换。

**代价必须一并接受：** 导航是缓存优先的，所以**普通刷新永远拿不到新版本**。「立即更新」是唯一的更新入口。

这个代价最初只由设置页的「应用与离线」一节承担，**结果真的踩到了**：M6 加完绘图之后，应用装着的还是旧版本，界面上根本找不到新加的按钮，而唯一能发现新版本的入口藏在设置页深处——用户不会没事去翻设置页。因为「更新」是唯一的更新路径，这个提示就不能只放在设置页。

**现在 `updateReady` 会在 `AppLayout` 里渲染成一条全应用可见的底部横幅**（`src/components/layout/AppLayout.tsx` 的 `UpdateBanner`）。做成布局里的一整行而不是 `fixed` 浮层，是因为侧栏底部是主题和设置按钮，浮层会压住它。已在浏览器里实测：**光是重新打开应用就会自动提示**（`register()` 对已存在的注册会触发一次更新检查），不需要先点「检查更新」。

**验证**：`npm run verify:pwa`，22 项断言。其中决定性的两条是「清空 HTTP 缓存后断网仍能打开」和「关掉浏览器重开、服务器全程没启动仍能打开」——后者不过，整个方案就不成立。

更新路径本身也在浏览器里实测过完整一轮：装上当前版本 → 服务器上换成新的构建 → **重新打开应用就自动出现横幅** → 点「立即更新」→ 旧缓存清掉、只剩新的、应用正常。这条以前一直没验证过（它需要两个不同的构建，手工做很别扭），而它是新版本进得来的唯一一条路。

---

## 8. 非功能需求

| 类别 | 要求 |
| --- | --- |
| **性能** | 笔记列表首屏 < 500ms；编辑器在 1 万字以上笔记中无输入卡顿；1000 篇笔记规模下搜索 < 1s |
| **可靠性** | 自动保存防抖 + 崩溃恢复（记录草稿，异常关闭后重新打开能找回未保存内容） |
| **数据安全** | 自动备份 + 一键导出必须与编辑功能同期上线（见 §7.4） |
| **隐私** | 数据完全不离开本机，无任何外部网络请求 |
| **易用性** | 从"打开 App"到"开始输入文字"不超过 2 次点击 |
| **兼容性** | Windows 10/11 上的 **Chrome / Edge 最新版**（Safari 和 Firefox 不是重点，因为 File System Access API 支持不全） |
| **深色模式** | 支持跟随系统 + 手动切换 |
| **输入效率** | 常用操作全部支持键盘快捷键，且**应用内能查到完整清单**（桌面产品的核心体验优势）；快捷键本身不该是「只有记得住的人能用」的功能 |

---

## 9. 开发里程碑

| 阶段 | 内容 | 预估周期 | 交付物 |
| --- | --- | --- | --- |
| **M0 项目骨架** | Vite + React + TS + Tailwind 初始化；IndexedDB schema 定义；`repository/` 数据访问层；路由与整体布局 | 2 天 | 项目跑起来，数据库可读写 |
| **M1 科目与章节** | 科目增删改查、科目列表页；章节增删改查、章节页；面包屑导航 | 2~3 天 | 能建科目、建章节，页面能跳转 |
| **M2 笔记编辑器** | Milkdown 编辑器接入；图片粘贴/拖拽/压缩/内联显示/预览；自动保存；笔记列表 | 3~4 天 | **可开始真实记录笔记**（关键节点） |
| **M3 检索与备份** | 全局搜索 + 高亮；一键导出/导入；自动备份到本地文件夹 | 2~3 天 | 数据有了兜底，可以放心长期使用 |
| **M4 打磨** | 深色模式；键盘快捷键；置顶/最近编辑；性能优化；空状态与错误处理 | 2~3 天 | 可长期使用的成品。**实际只做了键盘快捷键**（最先交付），其余拆到 M4.5 |
| **M6.5 机构符号** | GB/T 4460 符号库（10 个）；端点吸附；Shift 角度约束；机构语义（重合端点联动）；自定义符号（编辑模式 + 独立存储 + 进备份）；橡皮 | 2~3 天 | 能画规范的机构运动简图——**实际交付如左**。符号图形按标准的图形特征手绘，不是标准原图；线型规范仍未做 |
| **M5 增强（可选）** | 标签系统；Word/网页粘贴格式清理；单篇导出 PDF；表格/公式 | 3~5 天 | 按实际使用中的痛点选做 |

**核心路径累计约 2 周**（按每天有效开发 3~4 小时估算），M0~M3 约 9~12 天。

> ⚠️ **实施建议：M2 完成时就应该开始用它记真实笔记**，哪怕功能还很粗糙。后面的所有调整都应该基于"真的在用它"的反馈，而不是想象。

---

## 10. 待办与后续决策

技术方案已全部确定，无阻塞开发的前置决策。以下是实施过程中需要处理的开放项：

| # | 事项 | 说明 | 何时需要 |
| --- | --- | --- | --- |
| ~~D7~~ | ~~备份文件夹指向哪里~~ | ✅ M3 已实现，改为「用户在设置页选，建议指向 OneDrive 同步目录」 | 已解决 |
| ~~—~~ | ~~确认 Windows 上的目标浏览器~~ | ✅ 已确认 Edge（决策 D8） | 已解决 |
| — | 准备一个真实科目/章节做测试数据 | 用真实内容验证才能发现真问题 | 随时 |
| — | 导图功能是否重启 | 待 M4 完成后，根据实际使用体验重新评估 | M4 之后 |
| — | 镜像目录的清理 | 目前 `笔记/` 和 `images/` 只增不删，长期使用会堆残留文件。是否提供「清理孤立文件」按钮，等有实际空间压力时再定 | M4 之后 |
| — | 编辑器正文同步给保存层有 200ms 延迟 | 见 M4 实施记录第 2 条。目前不丢数据（1 秒后那次自动保存会补齐），只是「刚敲完立刻 Ctrl+S」存的是少几个字的版本。要不要让 Ctrl+S 先等编辑器同步完，等实际用起来觉得别扭再说 | 按需 |
| ~~—~~ | ~~离线缓存被清掉后无法启动~~ | ✅ 设置页写明了恢复办法（跑一次 `npm run serve`）。这是浏览器安全模型决定的，代码层面无解 | 已说明 |

> **下一步：** M4.5（深色模式打磨、置顶/最近编辑、性能、空状态）——M6.5（机构符号库、吸附、机构语义、自定义符号、橡皮）已完成。

### ⚠️ 开始认真记笔记之前，先做这三件事

M3 和 M3.5 已经交付，但**数据安全和使用体验取决于你有没有真的用起来**：

1. **装成桌面应用**：`npm run build && npm run serve`，在 Edge 里打开 `http://127.0.0.1:24816/` 点「安装」。装完之后双击任务栏图标就能开，不用再管服务器。**这个地址从此不要改**（理由见 §7.2）。
2. **到「设置与备份」里选一个备份文件夹**，指向 OneDrive / 坚果云的同步目录，并打开「自动备份」。只放在浏览器里 = 只存了一份。
3. **固定用这一个入口**。IndexedDB 是按浏览器 + 用户配置 + origin 隔离的：今天用 Edge 记、明天用 Chrome 打开，看到的是空的。装成应用之后就固定用它，不会搞混。

---

## 附录 A：思维导图功能设计（暂缓，保留设计稿）

> 本附录为 D4 决策暂缓的功能，设计已完整保留。待 M0~M4 完成、产品稳定使用一段时间后，再评估是否启动。

### A.1 功能需求（原 F6）

| 编号 | 需求 | 优先级 |
| --- | --- | --- |
| A1 | 在笔记详情页提供「生成思维导图」按钮 | P1 |
| A2 | 结果以可交互树状图展示（展开/折叠、缩放、平移） | P1 |
| A3 | 导图节点可手动编辑（改文字、增删节点、拖拽调整父级） | P1 |
| A4 | 导图与笔记关联保存，再次打开无需重新生成 | P1 |
| A5 | 笔记修改后提示"是否重新生成" | P1 |
| A6 | 按章节生成汇总导图（合并该章节下所有笔记） | P1 |
| A7 | 导出导图：PNG / SVG / Markdown 大纲 | P2 |
| A8 | 节点反向跳转到笔记原文位置 | P2 |

### A.2 三种生成方式

| 方式 | 原理 | 成本 | 质量 | 离线 |
| --- | --- | --- | --- | --- |
| **L1 本地规则解析** | 解析正文的标题层级与列表缩进，直接映射为树 | 0 | 取决于笔记写得多规整 | ✅ |
| **L2 LLM 语义生成** | 把笔记正文发给 Claude，要求输出结构化 JSON 树 | ≈ ¥0.1~0.4/篇 | 高：能识别概念、归纳层级、精炼文案 | ❌ |
| **L3 混合（推荐）** | 先用 L1 解析骨架 → 把骨架 + 原文一起给 LLM 做语义整理 | 略低于 L2 | 最高：结构不崩，又有语义增强 | 降级可用 |

**为什么推荐 L3：** 纯 L2 从零生成时偶尔会漏内容或自造结构；先给骨架相当于加了锚点，输出稳定得多。且 LLM 不可用时 L1 的结果可以直接顶上，用户不会看到"功能不可用"。

**实施顺序建议：先做 L1，再做 L3。** L1 能先把"渲染、交互、编辑、保存"整条链路跑通并验证；接入 AI 时只是替换"树从哪来"这一步，风险大幅降低。反过来先做 AI，一旦效果不理想，会分不清是生成的问题还是渲染的问题。

### A.3 LLM 调用设计

**形态**：单次请求（不需要 Agent / 工具调用 / 多轮），属于"文本 → 结构化输出"场景。

**模型选择**（Claude 系列当前定价，2026-09）：

| 模型 ID | 上下文 | 输入（每百万 token） | 输出（每百万 token） | 单篇笔记估算成本* |
| --- | --- | --- | --- | --- |
| `claude-opus-5` | 1M | $5 | $25 | ≈ $0.05（¥0.36） |
| `claude-sonnet-5` | 1M | $2 | $10 | ≈ $0.02（¥0.15） |
| `claude-haiku-4-5` | 200K | $1 | $5 | ≈ $0.01（¥0.08） |

\* 按单篇笔记 2000 汉字（约 3000 输入 token）+ 导图 JSON 约 1500 输出 token 估算。

**建议默认 `claude-opus-5`**（导图质量决定功能成败，单篇 ¥0.36 对个人使用可接受），并在设置里提供模型切换项。

**关键实现要点：**

1. **用结构化输出而非提示词约束格式。** 通过 `output_config.format` 传入 JSON Schema（对应 A.5 的 `graph` 结构），API 直接保证返回合法 JSON，**无需写"请只返回 JSON"、无需手工清洗**。
2. **开启自适应思考（`thinking: {type: "adaptive"}`）+ 中等推理强度（`output_config: {effort: "medium"}`）**，质量与延迟的平衡点。
3. **使用流式请求**，避免长 JSON 请求超时，并可实时展示"正在生成…"的进度。
4. **对同一篇笔记的重复生成启用提示词缓存**（把正文放在 prompt 前部并打 cache 标记），重复部分成本可降低约 90%。
5. **必须校验输出**：节点数 < 3 或 > 200、存在空 label、深度 > 6 层，均判为异常并降级到 L1。
6. **API Key 存放**：方案 A 下存 IndexedDB。需在设置页明确提示"Key 以明文存于本机"，并提供清除按钮。

### A.4 提示词草案

```
【系统提示】
你是一位擅长知识整理的助教。你的任务是把学生的课堂笔记整理成结构化的思维导图。

要求：
1. 只使用笔记中确实出现的信息，绝对不要补充笔记里没有的知识点。
2. 节点文案精炼，每条不超过 20 个汉字，去掉语气词和冗余修饰。
3. 层级控制在 3~4 层，每个父节点的子节点数量控制在 2~7 个。
4. 优先按"概念 → 定义/条件/结论/例子"的逻辑组织，而不是按笔记的书写顺序。
5. 如果笔记中包含公式，保留公式的关键部分（可用纯文本表示）。
6. 笔记中明确标为"重点""考点""不懂"的内容，在节点上做相应标记。

【用户消息】
科目：高等数学
章节：第三章 微分中值定理
笔记标题：2026-09-15 罗尔定理课堂记录

笔记正文：
---
（此处插入笔记原文）
---

请按给定的 JSON Schema 输出思维导图结构。
```

> 第 1 条是整个提示词里最重要的一句。**用户要的是"整理我的笔记"，不是"替我写一份知识总结"**——后者看起来更漂亮，但会让用户分不清哪些是自己记的、哪些是模型编的，从而失去对导图的信任。

### A.5 数据结构

```json
{
  "root": {
    "id": "n0",
    "label": "微分中值定理",
    "note": "可选补充说明",
    "children": [
      {
        "id": "n1",
        "label": "罗尔定理",
        "children": [
          { "id": "n1-1", "label": "条件：闭区间连续、开区间可导、端点等值" },
          { "id": "n1-2", "label": "结论：存在 ξ 使 f'(ξ)=0" }
        ]
      },
      { "id": "n2", "label": "拉格朗日中值定理", "children": [] }
    ]
  }
}
```

采用**树结构**（而非任意有向图）作为主存储格式：导图 95% 的场景是树；树渲染简单、可无损导出 Markdown 大纲、LLM 生成更稳定。未来若需"跨分支连线"，加一个平级的 `crossLinks: [{from, to, label}]` 字段即可，不破坏现有结构。

配套数据表（启用时新增）：

```
MindMap（思维导图）
  id            string   主键
  noteId        string   外键 → Note（章节汇总图时为 null）
  chapterId     string   外键 → Chapter
  scope         enum     'note' | 'chapter'
  sourceHash    string   生成时笔记内容的哈希（判断是否过期）
  graph         json     见上
  generator     enum     'llm' | 'local'
  modelId       string   使用的模型 ID
  createdAt     datetime
```

### A.6 渲染库选型

| 需求 | 推荐 |
| --- | --- |
| 快速出图、折叠/缩放/拖拽 | **mind-elixir**（轻量、零依赖、API 简单） |
| Markdown 文本直出导图 | **markmap**（与 L1 本地解析天然契合） |
| 节点内富文本、自定义节点组件 | **React Flow**（`@xyflow/react`，能力强但需自己写布局） |
| 导出 PNG/SVG | mind-elixir / markmap 内置；React Flow 需配 `html-to-image` |

**建议先用 mind-elixir**，开箱即用、交互完整，能最快验证功能价值。

---

## 附录 B：名词约定

| 术语 | 含义 |
| --- | --- |
| 科目 Subject | 最顶层容器，如"高等数学""数据结构" |
| 章节 Chapter | 科目下的分组，如"第三章 微分中值定理" |
| 笔记 Note | 具体的一篇记录，含标题、正文、图片 |
| PWA | Progressive Web App，可从浏览器"安装"为独立窗口应用的网页技术 |
| L1 / L2 / L3 | 见 A.2，三种导图生成方式 |

## 附录 C：参考信息

- Claude API 模型与定价：`claude-opus-5`（$5/$25 每百万 token）、`claude-sonnet-5`（$2/$10）、`claude-haiku-4-5`（$1/$5）
- 结构化输出：使用 `output_config.format` 传入 JSON Schema（旧的 `output_format` 参数已废弃）
- Markdown 即时渲染编辑器：Milkdown（推荐）、Tiptap
- 本地存储：Dexie.js（IndexedDB 封装）
- 本地文件读写：File System Access API（Chrome / Edge 支持）
- 思维导图渲染库：mind-elixir、markmap、React Flow
